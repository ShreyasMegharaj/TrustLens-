Flask==3.0.3
flask-cors==4.0.1
Werkzeug==3.0.3
numpy==1.26.4
opencv-python-headless==4.9.0.80
Pillow==10.3.0
# CPU-only torch — much smaller (~300MB vs ~2GB GPU version)
# Render free tier has no GPU so CPU-only is correct
torch==2.3.0+cpu
torchvision==0.18.0+cpu
gunicorn==22.0.0
python-dotenv==1.0.1
