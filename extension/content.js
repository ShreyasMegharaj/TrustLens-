/**
 * TrustLens Extension — Content Script
 * Injected into all pages. Currently minimal — reserved for future
 * features like hover-detection on images and page-level scanning.
 */

// No-op: context menu clicks are handled entirely in background.js + popup.js
// This script is here for future expansion (e.g., highlight suspicious images on page).
